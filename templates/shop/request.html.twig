{% extends 'base.html.twig' %}

{% block title %}Demande Boutique{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/auth.css') }}">
{% endblock %}

{% block body %}
<main class="auth-page">
    <div class="auth-container">
        <div class="auth-card">

            <div class="auth-logo">
                <h1>Playmate</h1>
                <p>Demande de compte boutique</p>
            </div>

            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}

            {{ form_errors(form) }}

            {{ form_start(form, {'attr': {'class': 'auth-form'}}) }}

                <div class="form-group">
                    {{ form_label(form.shopName) }}
                    {{ form_widget(form.shopName, {
                        'attr': {
                            'class': 'form-input',
                            'placeholder': 'Nom de votre boutique'
                        }
                    }) }}
                    {{ form_errors(form.shopName) }}
                </div>

                <div class="form-group">
                    {{ form_label(form.shopAddress) }}
                    {{ form_widget(form.shopAddress, {
                        'attr': {
                            'class': 'form-input',
                            'placeholder': 'Adresse complète'
                        }
                    }) }}
                    {{ form_errors(form.shopAddress) }}
                </div>

                <div class="form-group">
                    {{ form_label(form.shopPhone) }}
                    {{ form_widget(form.shopPhone, {
                        'attr': {
                            'class': 'form-input',
                            'placeholder': '05 00 00 00 00'
                        }
                    }) }}
                    {{ form_errors(form.shopPhone) }}
                </div>

                <button type="submit" class="btn-auth">
                    Envoyer la demande
                </button>

            {{ form_end(form) }}

        </div>
    </div>
</main>
{% endblock %}